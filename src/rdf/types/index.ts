import {
  BaseQuad, DataFactory, DatasetCore, DatasetCoreFactory, NamedNode, Quad,
// eslint-disable-next-line import/no-extraneous-dependencies
} from 'rdf-js';

export {
  BaseQuad, DataFactory, DatasetCore, DatasetCoreFactory, Quad, NamedNode,
};
